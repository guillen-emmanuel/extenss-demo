<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!--Email template -->
        <record id="extenss_request_email_template" model="mail.template">
            <field name="name">Quotation: Send by email</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and 'Quotation' or 'Order'} (Ref ${object.name or 'n/a' })</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
                        Hello,
                        <br/><br/>
                        <strong>Quotation ${object.name}</strong>
                        <strong>Name: </strong>${object.partner_id.name}
                        <strong>Product: </strong>${object.product_id.name}
                        <strong>Amount: </strong>${object.amount}
                        <strong>Start Date: </strong>${object.amount}
                        <strong>Interest Rate: </strong>${object.interest_rate_value}
                        <strong>Term: </strong>${object.term}
                        <strong>Payment Amount: </strong>${object.payment_amount}
                    </p>
                </div>
            </field>
            <field name="report_template" ref="report_extenss_request_sale_order"/>
            <field name="report_name">Quotation${object.name}${object.state == 'draft' and '_draft' or ''}</field>
        </record>
    </data>
</odoo>